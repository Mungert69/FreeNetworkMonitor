import React from 'react';
function BlogTitles({ titles }) {
    if (titles == undefined) {
        return null;
    }
    else {
        return (
            titles.map((item) => {
                <Link display="block" variant="body1" href={'#' + item.url} key={item.url}>
                    {item.title}
                </Link>
            })
        );
    }
}
export default BlogTitles;
